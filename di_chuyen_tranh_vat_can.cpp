#include <bits/stdc++.h>
using namespace std;

int n, a, b, c, d;
char a[101][101];
vector<int> lis(101);
bool visisted[101][101];
int dis[101][101];

void check() void makeList()
{
    for (int i = 0; i < n; i++)
    {
    }
}

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        cin >> n;
        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < n; j++)
            {
                char x;
                cin >> a[i][j];
            }
        }
        cin >> a >> b >> c >> d;
    }
}